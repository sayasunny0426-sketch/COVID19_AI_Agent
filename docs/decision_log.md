# Decision log

実装上の判断、除外理由、前処理、hyperparameter、random seed、モデル選択の理由を時系列で記録する。
過去の解析資料から取り入れた判断は、その出典と妥当性の検討結果も書く。
誤りの発見と修正は、別途 `change_log.csv`（[FB §12] の形式）に記録する。

出典の表記：[T] 課題文、[FB] 教員フィードバック、[U] 最初の依頼文、[R] 研究者の決定（2026-09-18 の追加指示）、[提案] Claude の提案

| ID | 日付 | 工程 | 判断内容 | 根拠 | 検討した代替案 | 判断に使ったデータ | 状態 |
|---|---|---|---|---|---|---|---|
| D-001 | 2026-09-18 | 計画 | 患者単位で Train / Val / Test に分割し、Test は最終評価専用とする | [U] [T] [FB §14] | – | なし | 確定 |
| D-002 | 2026-09-18 | 計画 | データと outputs を Git 管理外とする | データの利用規約と容量のため | – | なし | 提案（未承認） |
| D-003 | 2026-09-18 | 計画 | 分割比率を Train:Val:Test = 8:1:1 とする | [T] Lv.3 | 初期計画の 70/15/15、60/20/20 は取り下げ | なし | 確定 |
| D-004 | 2026-09-18 | 計画 | 実行環境を Google Colab（GPU T4 以上）とする。保存先は Google Drive | [T] [FB §12] | 初期計画の「OneDrive の外のローカル環境」は取り下げ | なし | 確定 |
| D-005 | 2026-09-18 | 計画 | CXR model は ResNet18（ImageNet の重みの転移学習）とする | [T] Task A | 初期計画の DenseNet121 と胸部 X 線で事前学習した重みは対象外 | なし | 確定 |
| D-006 | 2026-09-18 | 計画 | Clinical model は LR、XGBoost または LightGBM、MLP の 3 種をすべて実装する | [T] Task B | 初期計画の「LR と GBM の 2 種」は取り下げ | なし | 確定（XGB と LGBM のどちらにするかは未決定） |
| D-007 | 2026-09-18 | 計画 | LR は class weight なしと class weight ありの 2 条件で比較する | [FB §11] | – | なし | 確定（重みの指定方法は Q-C2） |
| D-008 | 2026-09-18 | 計画 | 統合モデルの主解析を Late Fusion とする | [FB §9] | Early Fusion（[T] では可、v1 の方式） | なし | 確定（仕様は Q-C4、副解析は Q-C5） |
| D-009 | 2026-09-18 | 計画 | 候補変数は臨床的知識と先行研究・既存 risk score に基づいて選び、根拠付きの候補表を作る。数は EPV 約 10 を目安にする | [FB §1, §2] | 初期計画の「事前指定を推奨」を具体化した | なし | 確定（EPV の数え方と stepwise は Q-B4） |
| D-010 | 2026-09-18 | 計画 | 統計的な外れ値を一律に除去しない。生理学的にあり得ない値のみを、事前に決めた規則で処理し、記録する | [FB §6] | **初期計画の「winsorization を Train で fit」は誤りとして削除** | なし | 確定 |
| D-011 | 2026-09-18 | 計画 | 補完の代表値（平均値か中央値か）は、Train の分布（ヒストグラム、Q–Q、Shapiro–Wilk）で変数ごとに根拠づける | [FB §4] | [T] Lv.2 の「平均値補完」は例示にとどまるため採用しない | なし | 確定（細部は Q-B7） |
| D-012 | 2026-09-18 | 計画 | T0 の第一案を入院時刻とし、すべての情報を T0 以前にそろえる。CXR 撮影時刻と入院時刻の差を監査してから最終決定する | [T] [FB §8] | 初期計画の「T0 後の許容範囲内の CXR も可」は取り下げ | なし | **D-020〜D-023 に置き換え** |
| D-013 | 2026-09-18 | 計画 | AUC の 95% CI は bootstrap 法で算出する | [T] Lv.3 | 初期計画の DeLong の CI は参考として併記を検討（Q-D2） | なし | 確定（反復回数は Q-D2） |
| D-014 | 2026-09-18 | 計画 | 多重比較の補正は Holm 法とする | [FB §10] | Bonferroni | なし | 確定（family は Q-D1） |
| D-015 | 2026-09-18 | 計画 | 保存構造は `baseline_v1_locked`（読み取り専用）、`revision_v2_*`、`00_governance` とし、`change_log.csv` と成果物の更新チェックリストを運用する | [FB §12] | – | なし | 確定（名前は Q-A3） |
| D-016 | 2026-09-18 | 計画 | CXR model は、事前に固定した複数の学習率（1×10⁻⁴ を含む）で学習し、全条件の履歴を保存する | [FB §7] | – | なし | 確定（範囲は Q-C1） |
| D-017 | 2026-09-18 | 計画 | 提出物に Grad-CAM（成功例と失敗例）、Feature Importance、学習曲線（Loss/Accuracy）、全モデルの ROC 曲線を 1 枚にまとめた図、AUC（95% CI）の表を含める | [T] | 初期計画にはこれらがなかった | なし | 確定（定義は Q-D3〜D5） |
| D-018 | 2026-09-18 | 計画 | 「fine-tuning」という用語は CXR model にのみ使う | [FB §7] | – | なし | 確定 |
| D-019 | 2026-09-18 | 研究デザイン | **v1 の固定コホート（1,299 例）と v1 の split は引き継がない**。v1 の数値は履歴としてのみ扱う | [R] | v1 のコホートを引き継ぐ案（[FB §14] の「コホート固定」をそのまま v1 に当てはめる案） | なし | 確定（Q-A1 を close） |
| D-020 | 2026-09-18 | 研究デザイン | **Prediction time point T0 = `visit_start_datetime`** | [R]。正確な admission time がデータに存在しないため | [FB §8] の第一案「入院時刻」はデータに存在しない | なし | 確定（Q-B1 を close） |
| D-021 | 2026-09-18 | 研究デザイン | **CXR は、撮影日（calendar date）が T0−2 日から T0 当日までのものを対象とする**。T0 より後の日付の CXR は使わない | [R] | v1 の「入院日 ±2 日」の窓（T0 後の +1, +2 日を含む）は使わない（[FB §8] の「T0 後は未来の情報」という指摘に対応） | なし | 確定 |
| D-022 | 2026-09-18 | 研究デザイン | **T0 当日の CXR を一律に「入院後」とはみなさない**（対象に含める）。正確な admission time がないため、T0 当日の CXR が admission の前か後かは判定できない。この点は研究上の limitation L-1 として記録する | [R] | T0 当日の CXR を除外する案は採らない | なし | 確定 |
| D-023 | 2026-09-18 | 研究デザイン | `AcquisitionTime` は**同じ日の中で画像の順序を決めるためだけに**使う。T0 との前後関係の判定には使わない | [R] | – | なし | 確定（D-025 で具体化） |
| D-024 | 2026-09-18 | コホート・split | **新しい固定コホートは 1,277 例**。固定 split は Train 1,021、Validation 128、Test 128。死亡数は Train 135、Validation 17、Test 17（合計 169）。これを以後の固定基準とする | [R]（D-020〜D-023 のルールで作成） | v1 のコホート（D-019 で不採用） | 研究者が作成（Claude は未検証） | 確定（manifest の hash の記録と数の照合はデータ監査で行う） |
| D-025 | 2026-09-18 | index CXR | **index CXR の選択ルール**：(1) 対象は eligible な frontal CXR のうち、撮影日（calendar date）が T0−2 日から T0 当日までのもの。(2) 撮影日が複数ある場合は、T0 に最も近い撮影日を選ぶ。(3) その撮影日に複数の Series がある場合は、`AcquisitionTime` が最も早い Series を選ぶ。(4) 最も早い `AcquisitionTime` が同じ Series が複数ある場合に限り、`SeriesNumber` が最小のものを選ぶ（再現性のための tie-breaker）。(5) T0 より後の日付の CXR は代わりとしても使わない | [R] | – | なし | 確定（Q-B6 を close。運用上の細部は Q-B9） |
| D-026 | 2026-09-18 | index CXR | `SeriesNumber` は撮影時刻や撮影順を示すものとは解釈しない。D-025 (4) の tie-breaker としてのみ使う | [R] | – | なし | 確定 |
| D-027 | 2026-09-18 | 保存先 | Google Drive 上の作業・保存ルートは既存の **「気合のCOVID19」フォルダ**とする。新しく `COVID19_mortality` は作らない。今回作る成果物はすべてこのフォルダの下に保存する | [R] | Claude が提案した `COVID19_mortality/`（取り下げ） | なし | 確定（中のフォルダ名は Q-A3） |
| D-028 | 2026-09-18 | index CXR | **frontal CXR の判定方法**：(1) DICOM の `Modality` が CR または DX。(2) `StudyDescription` で胸部検査の候補を特定する。(3) `SeriesDescription` に基づいて frontal の AP / PA に限定する。`ViewPosition` は主な判定基準にしない | [R] | `ViewPosition` を主な判定基準にする案（採らない） | なし | 確定（Q-B9(a) を close） |
| D-029 | 2026-09-18 | データアクセス | DICOM は現時点では別の Google Drive フォルダに複製しない。まず既存データの保存場所を確認してから、アクセス方法を決める | [R] | Claude が提案した `00_raw/` への配置（取り下げ） | なし | 確定（アクセス方法は Q-A4） |
| D-031 | 2026-09-18 | 臨床データ監査 | 監査の方法：split の manifest がない段階では、全 1,384 例に対して平均、中央値、分位点、歪度を計算しない（補完方法の判断材料になるため、Train で計算する）。全データで出すのは、件数、欠測、値の種類、最小値・最大値、事前に決めた範囲の外にある値の件数だけ。転帰は、その値の件数と visit 種別とのクロス集計にだけ使う。生理学的な範囲は暫定案であり、値の除去は行わない | [U] [FB §4, §6]、[提案] | 全データで分布を確認する案（リークの恐れがあるため採らない） | 全データ（ラベルは見ない） | 実施済み。**予備監査（D-035）** |
| D-032 | 2026-09-18 | 変数の分類 | `患者データまとめ、定義.csv` を data dictionary として、変数の意味・型・時点の判断に使う（`description`, `column_type`, `is_chart_abstracted`）。`column_counts` は 1,479 例の数で、臨床ファイル（1,384 例）と一致しないため、欠測率の計算には使わない。辞書に `use_category` 列はなく（研究者が確認）、過去の使用／不使用との比較は行わない。候補変数の A/B/C 区分は Claude の独立した暫定案（臨床的知識、COVID-19 死亡に関する先行研究、既存 risk score、T0 時点の利用可能性、欠測、リーク、重複に基づく） | [U] [R] [FB §2]、[提案] | – | なし（全データでの欠測率は記述統計としてのみ参照） | 実施済み。**予備監査（D-035）**。正式な分類は Train での再監査の後に行う |
| D-033 | 2026-09-18 | 候補変数 | `docs/preliminary_pre_cohort_audit/candidate_variables_draft.csv`（AI が独立に作った初期候補：A 22 / B 19 / C 21）を記録として保存し、上書きしない（SHA256 `6ac5c482ffcce1be…2a2d7bfd`。D-035 のフォルダ移動の後も同じ値）。`scripts/03b` はこのファイルがあれば書き換えない | [R] | – | なし | 確定（予備監査の記録） |
| D-034 | 2026-09-18 | 候補変数 | stepwise の前の shortlist **案**：A の 22 変数から、推奨 13 変数（約 14 パラメータ）、代替 8、除外 1。優先順位は、臨床的妥当性 > SR/MA・既存スコア > T0 付近で利用可能 > 冗長性の少なさ > 欠測 > 検査オーダーへの依存。転帰との関連と Val/Test の情報は使っていない。新しい派生変数（併存症の数、NLR）や recoding は採用せず、別案として示すにとどめる。欠測率は全データの記述統計しかなく、欠測を判断に使った 6 変数は Train で確認し直す必要がある | [R] の指示、[FB §1–3]、[提案] | 別案 1〜6（preliminary_pre_cohort_audit/candidate_shortlist_prestepwise_draft.csv） | なし（欠測率は全データの記述統計） | **保留（D-035）。予備監査の記録としてのみ保存し、最終的な選定には使わない** |
| D-035 | 2026-09-18 | 作業順序 | **方針の変更**：臨床データの監査と候補変数の選定を保留する。今後の順序は、(1) CXR 画像監査 → (2) index CXR 選定 → (3) 最終コホートの固定 → (4) Train / Validation / Test split の固定 → (5) Training set だけで臨床データを再監査 → (6) 候補変数の選定 → (7) 欠測補完・変換 → (8) clinical model の構築。これまでの臨床側の成果物（D-031, D-032, D-033, D-034）は `docs/preliminary_pre_cohort_audit/` に「CXR コホート固定前の予備監査」として保存し、最終的な変数選定や前処理の決定には使わない。外付け SSD が使えるようになるまで、CXR 監査も臨床側の追加解析も行わない | [R] | – | なし | 確定 |
| D-036 | 2026-09-18 | コホート・split | **Q-A6 は (a) を採る**。(1) SSD を接続した後、確定済みの CXR 選定ルール（D-020〜D-023, D-025, D-026, D-028）からコホートを作り直す。(2) 作り直したコホートが既存の最終コホート 1,277 例と完全に一致するか照合する。(3) 一致した場合は、既存の固定 split（Train 1,021 / Validation 128 / Test 128）をそのまま使う。(4) split は作り直さない。(5) 一致しない場合は、split にも後続の解析にも進まず、差分の患者、除外理由、画像選定ルールのどの段階で差が出たかを報告する。(6) 差分を推測で補正しない | [R] | (b) コホートと split を新たに作る（採らない） | なし | 確定（Q-A6 を close） |
| D-037 | 2026-09-18 | コホート・split | **照合の合格条件（Q-A7）**：(1) 最終コホートの患者集合が既存の 1,277 例と完全に一致すること。(2) 既存 manifest に index CXR の識別子がある場合は、患者ごとの index CXR も完全に一致すること。照合には StudyInstanceUID、SeriesInstanceUID、SOPInstanceUID、撮影日のうち、manifest にあるものをすべて使い、1 つでも食い違えば不一致とする（manifest に index CXR の識別子がない場合は (1) だけで判定し、そのことを記録する）。**患者集合が一致していても、index CXR が異なる患者が 1 例でもあれば、後続の解析に進まず停止する**。停止した場合は差分表（subject_id、既存の index CXR、再構築した index CXR、撮影日、選択ルールのどの段階で差が出たか）を報告する。差分を推測で補正したり、既存 manifest に合わせて選び直したりしない | [R] | 患者集合の一致だけで判定する案（採らない） | なし | 確定（Q-A7 を close） |
| D-038 | 2026-09-20 | CXR 監査 | **StudyDescription の分類**：`CODE T CHEST AP PORTABLE` と `CHEST AP INFANT PORTABLE` を **chest_candidate** とする。名称だけを理由に除外せず、frontal CXR としての適格性で判断する。患者側の適格性は患者選定の段階で評価する。`TRAUMA CHEST AP PORTABLE` は別扱いで exclude_special_purpose のまま。結果、chest_candidate 9 種類 / exclude_special_purpose 3 種類 / exclude_non_chest 7 種類 / needs_review 0 種類 | [R] | needs_review のまま保留する案（採らない） | 全患者の DICOM ヘッダー（ラベルは見ない） | 分類は確定。対応表全体は確定前（承認待ち） |
| D-039 | 2026-09-20 | index CXR | **CXR の inclusion window を T0−2 日〜T0 当日に固定する**。再構築の結果、T0 以前／当日に eligible な CXR がある 1,309 例のうち、窓に入る 1,277 例を採用し、最も近い eligible CXR が T0 から 3 日以上前になる 32 例を除外する | [R]（D-021 の窓を、再構築の分布を見て確定） | 窓を広げて 32 例を含める案（採らない） | 全患者の DICOM ヘッダーと T0（ラベルは見ない） | 確定 |
| D-040 | 2026-09-20 | index CXR | 再構築した 1,277 例の index CXR manifest を次工程の基準として保存する（`data/interim/cxr_audit/index_cxr_manifest_window_T0m2_T0.csv`、SHA256 `e8c07db7b7caac8d8e04960b6c80ca83b47adf16d5f426f0f786ba2f40f0743b`。初回保存分の `a8d79bfe…` は AcquisitionTime の桁落ちがあり、CL-002 で修正した）。窓を適用する前の 1,309 例の候補 manifest（`index_cxr_candidate_manifest.csv`）は上書きせず保持する。**人数が 1,277 で一致した段階であり、既存コホートとの患者 ID・index CXR の照合（D-036, D-037）はまだ行っていない。コホートの確定ではない** | [R] | – | – | 保存済み（照合は未実施） |
| D-041 | 2026-09-20 | コホート照合 | **D-036 / D-037 の合格条件を満たした**：既存の選択済み DICOM 1,277 枚（`<LOCAL_PATH> Drive\気合のCOVID19\01_TaskA_CXR\01_前処理\selected_dicom_1277`、読み取り専用）と再構築 manifest を照合し、患者集合が完全一致（片側のみの患者 0）、全 1,277 例で SeriesInstanceUID・SOPInstanceUID・StudyInstanceUID・撮影日・AcquisitionTime・SeriesNumber・Description・Modality がすべて一致した。既存側に合わせる調整は行っていない | [R] の照合指示、D-036, D-037 | – | 既存 DICOM ヘッダーと再構築 manifest（ラベルは見ない） | 照合は合格。**コホート固定の宣言と split 照合は未実施（split manifest の最新版が未特定）** |
| D-042 | 2026-09-20 | split | **split 照合を保留する**。患者 ID ごとの割付（Train 1,021 / Validation 128 / Test 128）を持つ最新の split manifest が特定できていないため。保留中は、**新しい split を作らない／seed を決め直さない／Train・Validation・Test を再割付しない**。ローカル検索（`<LOCAL_PATH> Drive\気合のCOVID19`）では、`selected_dicom_1277` の DICOM 1,277 枚と `CXR_1277_DICOMコピー監査ログ.csv`（列は Subject ID, Series UID, status, source, dest で split 情報を含まない）しか見えていない。Drive 上には他の成果物が存在する | [R] | 新たに split を作る案（採らない） | – | 保留（manifest 特定待ち） |
| D-043 | 2026-09-20 | split | **既存の固定 split を正式採用する**（D-036 の手順に合格）。ファイル `COVID19_固定患者split_1277.csv`、SHA256 `626061a532ab7fa05eeb0198019222e8919d04e5e4df720230538149389599f8`（コピー後も同一）。照合結果：Subject ID 集合が照合済みコホート 1,277 例と完全一致（片側のみ 0）、split 間の患者重複 0、人数 Train 1,021 / Validation 128 / Test 128、死亡数 135 / 17 / 17、`Subject ID` と `to_patient_id` が全例一致、`last.status` と `true_label` の矛盾 0、臨床ファイルの転帰と 1,277 例すべてで一致。**新しい split は作成せず、seed も決めていない。今後の全 Task でこの split を共通で使い、再割付は行わない**。プロジェクト内の保管先は `data/splits/COVID19_固定患者split_1277.csv`（hash ファイルを併置） | [R]、D-036 | split を作り直す案（採らない） | 割付の照合のみ（予測変数は見ていない） | 確定（D-042 の保留を解除） |
| D-044 | 2026-09-20 | Task A 前処理 | **Task A の前処理仕様を固定し、キャッシュを作成した**：RescaleSlope/Intercept を適用（本データでは恒等）→ MONOCHROME1 なら反転（該当 0）→ **DICOM の WindowCenter/Width は使わず**、画像ごとの 1–99 percentile clip → [0,1] 変換 → 長辺に合わせて zero padding（縦横比を保持）→ 512×512 に bilinear リサイズ → uint16 量子化 → 16 bit PNG。foreground crop と rotation correction は行わない。**データセット統計を使わないため、Train で fit する対象がない**。保存先は `data/processed/taskA_png512_16bit/`（既存の `processed_png_1277` には触れていない）。round-trip QC は 1,277 枚すべてで完全一致（max_abs_diff 0、uint16、512×512） | [R] の承認（2026-09-20）、task_a_implementation_plan §2 | 既存 Task A の前処理仕様に合わせる案（研究者指示により Task A 完了まで参照しない） | 全 1,277 枚（画像ごとに閉じた処理のため split 情報は不要） | 確定（学習は未着手） |
| D-045 | 2026-09-20 | Task A 計画 | 計画の 5 点修正：(1) 既存 `processed_png_1277` と既存 Task A の詳細仕様は、エージェントの Task A 完了まで参照しない（監査は最終比較フェーズ）。(2) 16 bit PNG の round-trip QC を必須とし、想定外なら停止して報告する。(3) Grad-CAM の症例選択は TP・FP を確率の高い順に 4 例、TN・FN を確率の低い順に 4 例とし、同値は PatientID 昇順で一意に解決する。(4) seed ensemble を採る場合も、各 seed 単独の患者単位予測と性能を保存し、ensemble の予測は別ファイルにする。(5) 層別 bootstrap の理由は「少数イベントの Test set で class composition を保ち、CI 推定を安定させるため」とする | [R] | – | – | 確定 |
| D-046 | 2026-09-20 | Task A 成果物 | 前処理キャッシュを Drive の `01_TaskA_CXR/01_前処理/AIagent_taskA_png512_16bit` に**別成果物として保存**（既存 `processed_png_1277` は上書きしていない）。内容：`images/`（1,277 枚）、`preprocess_records.csv`、`preprocess_summary.json`、`scripts/06_taskA_preprocess_cache.py`、`file_manifest.csv`（全 1,279 ファイルの SHA256）、`source_hashes.json`（入力ファイルの hash と pipeline 設定）、split manifest と index CXR manifest の写し。コピー後に全ファイルの hash を再計算し、1,279 件すべて一致を確認 | [R] | – | – | 確定 |
| D-047 | 2026-09-20 | Task A Dataset | **Dataset / DataLoader 仕様を確定**：1 患者 1 画像、固定 split をファイルから読むのみ（再作成しない）、16 bit PNG → float32/65535 → （Train のみ）augmentation → 224×224 bilinear → 3 channel 複製 → ImageNet 正規化。Val/Test は augmentation を構造的に禁止（コンストラクタで例外）、DataLoader も非シャッフルで決定的。`pos_weight` は Train 以外で計算すると例外。QC 29 項目すべて合格（全 1,277 枚の読み込み確認を含む）。ResNet18（IMAGENET1K_V1、`Linear(512,1)`）の結線確認も 9 項目合格 | [R] の承認、task_a_implementation_plan §3–4 | – | Train/Val のみ（Test は件数・ラベル整合性の確認のみで、画像は QC の読み込み確認に使用） | 確定（学習は未実施） |
| D-048 | 2026-09-20 | Task A 実行環境 | **学習ロジックを再利用可能なモジュールとして実装し、Colab Notebook は薄い実行インターフェースにする**。構成：`src/covid_mortality/training/taskA_trainer.py`（学習エンジン）、`src/covid_mortality/evaluation/metrics.py`（numpy のみ。sklearn に依存せず環境間で数値が揺れない）、`scripts/09_taskA_run_training.py`（事前登録した grid の展開、完了 run の skip、再開）、`notebooks/TaskA_train_colab.ipynb`（Drive mount、GPU 確認、install、config、実行、結果確認のみ）。CUDA の有無で環境を自動判定し、Colab 以外の GPU 環境でも同じコマンドで再実行できる。run ごとに config.json / history.csv / best.pt / last.pt / val_predictions.csv / DONE.json を**各 epoch 終了時に Drive へ逐次保存**し、切断後は再実行するだけで再開する。設定を変えたまま再開しようとした場合は例外で停止する（別 run として扱う）。smoke test：Dataset QC 29 項目、モデル結線 9 項目、指標 10 項目、再開・skip 8 項目すべて合格 | [R] の指示、task_a_implementation_plan | Notebook に学習ロジックを書く案（採らない） | Train/Val のみ（Test は未使用） | 確定（本学習は未開始） |
| D-049 | 2026-09-20 | 評価指標 | **AUPRC の定義を Average Precision（step-wise）に固定する**：AP = Σ(R_k − R_{k−1})·P_k を distinct threshold 単位で計算する（scikit-learn の `average_precision_score` と同一）。**PR 曲線の台形積分（`auc(recall, precision)`）は使わない**。台形積分は点間を補間するため、イベントが少ない場合に楽観的になりやすい。AUROC は同順位に mid-rank を与える Mann–Whitney U 統計量（`roc_auc_score` と同一）。以後 Task A・B・C のすべてでこの定義を使う | [R] の確認指示、[T] | 台形積分の PR-AUC（採らない） | 合成データのみ | 確定（参照照合済み。CL-004） |
| D-050 | 2026-09-20 | Task A 保存先 | run の出力先を Drive の `01_TaskA_CXR/04_Training/AIagent_taskA_runs` とする（既存 Task A のフォルダ構成に合わせ、`02_学習` は作らない）。既存の学習成果物は参照も上書きもしない。コード一式は `00_共通・研究管理/AIagent_code` に置く | [R] | Claude が提案した `02_学習` フォルダ（取り下げ） | – | 確定 |
| D-051 | 2026-09-21 | Task A 実行 | **コードの配布方式を ZIP に変更**：`00_共通・研究管理/AIagent_code/AIagent_code_20260921.zip`（SHA256 `bdb2f47da150d00e761e7ef41ac9072e2a7da530cf51d52526e8ec02efc7b481`。ZIP は `scripts/11_build_code_bundle.py` が POSIX 区切りで作成し、entry 名・UTF-8・必須ファイル・Linux 方式の展開を自動検証する。旧 `AIagent_code_20260920.zip` は CL-005 の不具合のため使用しない）を Colab 側で `/content/code` に展開して実行する。Drive 上の ZIP は読み取るのみで変更しない（展開前後で hash が変わらないことを Notebook 内で確認する）。**Notebook 自身は ZIP に含めない**（含めると Notebook を修正するたびに hash が変わり、整合性確認が循環するため）。学習条件、LIMIT=1、保存先、Stage 1 の設定は変更していない | [R] | Drive に展開済みのコードを置き rsync する方式（前案） | – | 確定（ローカルで展開・実行の疎通を検証済み） |
| D-052 | 2026-09-21 | Task A Stage 1 | **Stage 1（24 run）完了**。3 seed 平均 Val AUROC の最良は `lr3e-4_aug_b`（0.872637 ± 0.004569、best epoch は seed42=9 / seed43=5 / seed44=14）。総計算 1.45 時間、平均 3.6 分/run。24 run のうち best epoch=1 だったのは `lr3e-4_aug_a`/seed42 の 1 件のみで、v1 で問題になった「epoch 1 が best」は再現していない。**この時点では最終モデルを確定しない** | [R] の報告値 | – | Train/Val のみ | 記録（選択は Stage 2 後） |
| D-053 | 2026-09-21 | Task A Stage 2 | **Stage 2 の 2 条件を事前固定**（`docs/task_a_stage2_design.md`、研究者承認 2026-09-21）：S2-A `lr3e-4_aug_b_warmup1`（epoch 1 head のみ → epoch 2 以降 全層）、S2-B `lr3e-4_aug_b_staged`（epoch 1 head → epoch 2–3 head+layer4 → epoch 4 以降 全層）。いずれも Stage 1 最良条件から**解凍方法だけ**を変え、学習率 3e-4・aug_b・3 seed・その他の設定は同一。**Stage 2 専用の学習率スケジュール**：optimizer を fc / layer4 / rest の param group に分け、各群は凍結中 0、解凍後 1 epoch で 0→3e-4 に立ち上げ、その後 cosine 減衰。これにより解凍直後の backbone に最大学習率が入らない（実測：解凍直後 9.37e-06）。**Stage 1 のコード経路・run・成果物は変更しない**（`unfreeze_schedule=None` のとき従来の OneCycleLR のまま） | [R] の承認 | 2 位条件への warm-up 適用、discriminative LR、条件数の増減（いずれも不採用。設計書 §7） | Train/Val のみ | 実装・検証済み（実行待ち） |
| D-054 | 2026-09-21 | Task A 選択規則 | **Stage 2 の採否規則を実行前に固定**：mean Validation AUROC の改善が Stage 1 baseline に対して **0.005 未満の場合は、事前に固定した practical tolerance / parsimony margin として扱い、より単純な Stage 1 条件（`lr3e-4_aug_b`）を採用する**（「統計的に区別できない」とは表現しない）。0.005 以上改善した場合は mean Val AUROC 最大の条件を候補とし、近接する条件については seed 間 SD、Val AUPRC、Val loss、学習曲線、best epoch の分布も確認して決める。結果を見てから規則を変えない | [R] の指示 | – | Val のみ | 確定 |
| D-055 | 2026-09-21 | Task A 学習条件 | **最終学習条件を `lr3e-4_aug_b`（Stage 1 baseline）に確定**。Stage 2 の結果：warmup1 は mean Val AUROC 0.869811 ± 0.008043（baseline 比 **−0.002826**）、staged は 0.856209 ± 0.008112（**−0.016428**）。D-054 の規則 1（改善が 0.005 未満なら practical tolerance / parsimony margin として扱い、より単純な Stage 1 条件を採用）に該当。warmup1 は Val AUPRC が baseline より高い（0.556 対 0.537）が、**主評価は事前に mean Val AUROC と決めており、副次指標を理由に規則を変えない**。baseline は seed 間 SD が最小（0.004569）で、best epoch も 5/9/14 と早期収束していない。全 30 run と比較表は `docs/task_a_condition_selection.md`、`task_a_condition_comparison.csv`、`task_a_condition_comparison.decision.json` に保存し、run 成果物からの再生成は `scripts/13_taskA_condition_comparison.py` で行える | [R] の決定、D-054 の規則 | Stage 2 の 2 条件（いずれも不採用） | Train/Val のみ（**Test 未使用**） | 確定 |
| D-056 | 2026-09-21 | Task A 最終モデル | **主解析の CXR モデルは単一 seed とし、`lr3e-4_aug_b / seed 42` を事前固定する**。seed 43 / 44 の個別結果は再現性確認の副次解析、3 seed ensemble も副次解析として保存・評価するが、**主解析には使わない**。「確率的モデルはすべて ensemble」という原則は現時点では採用しない。**理由**：本研究の目的の一つが、従来の Task A（seed 42 単一モデル）と AI agent が独立に設計した Task A の公平な比較であり、agent 側だけを 3 seed ensemble にすると学習条件の違いと ensemble の効果が混ざって直接比較が難しくなるため | [R] の決定 | Claude が推奨した 3 seed ensemble を主解析とする案（不採用） | Train/Val のみ（**Test 未使用**） | 確定 |
| D-057 | 2026-09-21 | Task A 閾値 | **分類閾値の規則を実行前に固定**（Q-D3 を close）。**主解析**：Validation で Youden index が最大となる閾値を事前固定し、**Test では再計算しない**。**同率の場合の tie-break は「最も低い閾値」**とする（J が同じなら感度が高くなり、死亡の見落としが少なくなるため）。**副次の操作点**：「Validation 感度 ≥ 0.80 を満たす中で**最も高い**閾値」（感度 0.80 以上を保ちつつ特異度を可能な限り高くする）。感度 0.80 には現時点で明確な臨床的根拠がないため、**exploratory secondary operating point** として扱い、主解析には用いない | [R] の決定 | 「感度 0.80 を満たす最小の閾値」（Claude の当初案、修正済み） | Val のみ（**Test 未使用**） | 確定 |
| D-058 | 2026-09-21 | Task A Test 評価 | **凍結と Test 評価の実装・事前検証を完了**（実行は未）。`scripts/14_taskA_freeze_selection.py` が `final_selection.json` を作成（主解析 seed 42、閾値 2 種、副次の per-seed と 3 seed ensemble、全 checkpoint・config・Val 予測・manifest の SHA256）。`scripts/15_taskA_test_evaluation.py` は、凍結ファイルの存在と全 hash 一致、Test 128 例・死亡 17 例・split 重複なしを確認したうえで 1 回だけ実行でき、`--dry-run` で同じ経路を Validation に対して検証できる。`scripts/17_taskA_eval_pipeline_check.py`（当初 16 番。比較スクリプトと番号が衝突したため改番）による事前検証 20 項目すべて合格（凍結なしでの拒否、閾値が凍結値と一致、患者単位 128 行、層別 bootstrap CI、per-seed と ensemble と L-1 感度分析の出力、再実行の拒否とログ記録、checkpoint 改変時の停止） | task_a_test_evaluation_plan.md | – | Val のみ（**Test 未使用**） | 実装・検証済み（Test 実行待ち） |
| D-059 | 2026-09-21 | Task A 比較 | **current model と AI-agent model の比較を post-hoc / exploratory comparison として位置づける**。事前登録した主要仮説検定ではないため、p 値は多重比較未補正で、確認的な結論には使わない。「current model が統計学的に優れている」と断定せず、同一 Test 128 例での exploratory paired comparison として記述する。比較は既存の Test 予測のみを使い、再学習も Test の再評価も行わない。paired DeLong は `src/covid_mortality/evaluation/delong.py`（DeLong 1988 / Sun & Xu 2014 の高速実装、同順位は midrank、numpy のみ）に実装し、単体テスト 7 項目合格（AUC が既存実装と一致、bootstrap SE と整合：0.0411 対 0.0403、同一予測で p=1、符号の対称性、CI と p の整合、同順位の処理） | [R] の指示 | – | Test 予測（既存。再評価なし） | 実装・検証済み（実行は Colab 側） |
| D-060 | 2026-09-21 | Task A Grad-CAM | **凍結済み primary model（`lr3e-4_aug_b` / seed 42）に対する Grad-CAM を実装**（`scripts/18_taskA_gradcam_primary_test.py`、`src/covid_mortality/evaluation/gradcam.py`）。対象層は ResNet18 `layer4[-1]`。閾値は `final_selection.json` の Youden 値をそのまま使い、**Test で再計算しない**。症例選択は D-045 のとおり TP・FP を確率の高い順に 4 例、TN・FN を低い順に 4 例、同値は Subject ID 昇順。実行前に、凍結ファイルの primary model が `lr3e-4_aug_b/seed42` であること、checkpoint の hash 一致、Test 予測 128 例、各群 4 例以上、16 例すべての画像の存在を確認し、満たさなければ停止する。出力は一時領域で作り切ってから移すため、失敗時に中途半端な成果物を残さない。CAM の質量が padding / 外周帯 / 中央 50% のどこに落ちたかを定量化し、焼き込み文字や余白への注目を数値でも確認できるようにした（解剖学的な読影は研究責任者が記入する前提）。サンドボックス検証 15 項目＋事前チェック 4 項目すべて合格 | [R] の指示、D-045 | – | Test 予測（既存。再評価なし） | 実装・検証済み（実行は Colab 側） |
| D-061 | 2026-09-21 | Task A Grad-CAM | **症例選択規則の修正**：本番 Test では凍結閾値のもとで FN が 3 例しか存在しないため、TP・FP・TN は従来どおり各 4 例、**FN は存在する 3 例すべてを採用し、合計 15 例**とする。**16 例に合わせるために他群から追加症例を選ばない**。4×4 パネルは 1 セルを空欄（`(no case)`）のままにする。実行前チェックは TP≥4・FP≥4・TN≥4・**FN≥1** とし、FN は `min(4, available)` を採用する仕様に変更。この経緯は `gradcam_selection.csv`（`selection_note` 列）、`gradcam_notes.md`（冒頭と FN 節）、`gradcam_run_meta.json`（`selection_note`、`cases_available_per_group`、`panel_empty_cells`）に明記する。その他の仕様は変更していない。サンドボックス検証 11 項目合格（15 例、群構成 4/4/4/3、FN 3 例すべて採用、他群が増えていないこと、3 つの出力への注記、空セルの描画） | [R] の指示 | 他群から 1 例補充して 16 例にする案（採らない） | Test 予測（既存。再評価なし） | 確定（実行は Colab 側） |
| D-062 | 2026-09-21 | Task A Grad-CAM | **Grad-CAM 実行専用 Notebook を作成**（`notebooks/TaskA_gradcam_colab.ipynb`、12 コードセル）。Drive mount → パス定義 → ZIP の SHA256 検証 → `/content/COVID19_AI_Agent` へ展開 → 環境確認 → 入力の存在確認 → 凍結済み primary model（`lr3e-4_aug_b`/seed42）と Youden 閾値の確認 → `scripts/18` の実行 → 出力確認（15 例・TP4/FP4/TN4/FN3）→ メタ情報 → 4×4 パネル表示 → サマリー。**ZIP の期待 SHA256 は同梱の `<zip名>_manifest.json` から読む**（固定値は fallback）。これにより ZIP を更新しても Notebook 内の hash が古くならず、Notebook を ZIP に同梱しても hash の自己参照が起きない。学習用 Notebook も同じ方式に統一した。配布 ZIP に `notebooks/` を含めるようビルド設定を変更（`AIagent_code_20260921i.zip`）。i は h に notebooks を加えたもので、`src/` と `scripts/` の内容は同一であることを確認済み。Notebook 検証 24 項目合格 | [R] の指示 | Notebook 内に ZIP の hash を固定値で持つ案（版更新のたびに古くなるため manifest 方式を採用） | – | 確定（実行は Colab 側） |
| D-063 | 2026-09-21 | Task A Grad-CAM | **Grad-CAM の読影総括を研究者が記入し、`gradcam_notes.md` に追記する**（`scripts/19_taskA_append_gradcam_notes.py`）。追記は append のみで、既存の症例別メモ・定量情報・実行記録は削除しない。`gradcam_run_meta.json`、`gradcam_selection.csv`、パネル、個別画像は変更しない。再学習・再評価・閾値の再計算は行わない。所見は**定性的解釈**であり、Grad-CAM の強調領域が予測の因果的根拠であるとは断定しない旨を本文に明記。既存の `## 総括（読影）` の下に `### 読影総括（研究者記入）` として追加し、見出しの重複を避ける。追記前の SHA256 と追記日時を本文末尾に残し、二重追記は拒否する。検証 19 項目合格（既存内容の完全保持、見出し重複なし、二重追記の拒否、ファイル不在時の停止） | [R] の指示 | – | Grad-CAM 成果物（生成済み。再実行なし） | 確定（実行は Colab 側） |
| D-030 | 2026-09-18 | index CXR | `AcquisitionTime` が欠けている場合の扱い（Q-B9(b)）と、選んだ Series に複数の画像がある場合の扱い（Q-B9(c)）は**推測で決めない**。データ監査で該当する症例数を確認し、該当例がある場合に限り、研究責任者に判断を求める | [R] | – | なし | 手順は確定（扱いは未決定） |
| D-064 | 2026-09-22 | GitHub 提出 | **GitHub 提出用 repo に Drive 正本の最終成果物を取り込んだ**（`scripts/20_prepare_github_repo.py --drive-root`）。Test 評価 10 ファイル、比較 4 ファイル（ZIP から展開）、Grad-CAM 4 ファイル（`gradcam_panel_4x4.png` を含む）を `results/taskA/{evaluation,comparison,gradcam}` に収録。**Drive 側は読み取りのみで変更していない**。README の Test 性能・DeLong 比較・Grad-CAM の数値は**すべて収録した実ファイルから読み出して生成**するようにし、「研究者報告・未検証」の表記を削除した。数値の出典は `docs/results_provenance.md`、正本との対応と SHA256 は `results/taskA/README.md` に記録。再学習・Test 再評価・閾値の再計算・モデル再選択は行っていない | [R] の指示 | – | 既存の Test 成果物（再評価なし） | 確定 |
| D-065 | 2026-09-22 | GitHub 提出 | **公開版から DICOM instance UID 3 列（StudyInstanceUID / SeriesInstanceUID / SOPInstanceUID）を除去**する。TCIA COVID-19-NY-SBU の再配布条件を本作業環境から確認できなかったための保守的措置（参照範囲が Drive の「気合のCOVID19」配下に限定されているため外部の規約ページを参照していない）。対象は `index_cxr_manifest_1277.csv` と `gradcam_selection.csv`。**完全版は Drive の正本に残っており、その SHA256 を `results/taskA/README.md` に記録**したうえで、`scripts/02b` と `scripts/18` から再生成できる。`PUBLISH_UIDS = True` に変更すれば収録に戻せる。**研究結果・数値・モデル選択・閾値には一切影響しない** | [R] の指示（「再配布条件を確認できない場合は除外し、再現方法と hash のみ残す」） | UID をそのまま公開する案（規約未確認のため採らない） | – | 暫定（Q-E1 の回答待ち。push 前に再判断） |
| D-066 | 2026-09-22 | GitHub 提出 | **Grad-CAM の個別画像 15 枚と群別パネル 4 枚は公開版に含めず、4×4 パネル 1 枚のみを収録する**。理由：4×4 パネルに 15 症例すべてが含まれており、個別画像はその高解像度版で新しい情報を加えない（約 2.4 MB でリポジトリが 3 倍になる）。ファイル名・サイズ・SHA256 は `results/taskA/gradcam/gradcam_excluded_images_sha256.csv` に残し、`scripts/18` で再生成できる | [R] の判断委任 | 全 19 枚を含める案、パネルも含めない案（いずれも採らない） | – | 確定 |
| D-067 | 2026-09-22 | コード正本 | **コードの正本版は `AIagent_code_20260921j.zip`（2026-09-21 23:35、345,442 bytes、SHA256 `55531ab56c171e8bb4cbd2cb5514c9091888e156687de9f888e6fdea4ee3ce8f`、69 entry）で確定**。`00_共通・研究管理/AIagent_code/` に j より新しい ZIP は存在しない。j の全 entry をローカル作業ツリーと byte 単位で照合し、**内容の差分は 0**（ローカルにのみ `scripts/20_prepare_github_repo.py` があり、これは j の作成後に追加した GitHub 整理用スクリプト）。同フォルダの `TaskA_gradcam_colab.ipynb`（2026-09-22 08:10）と `TaskA_train_colab.ipynb`（2026-09-21 20:00）は **Colab 実行後の出力入り単体コピー**であり、正本の Notebook ではない（出力 9 個を保持、末尾に実行セッション用のセル 2 つを追加）。公開版には ZIP 内の出力なし Notebook を採用した | 実ファイル照合 | Drive 側 Notebook を採用する案（出力入りのため採らない） | – | 確定 |
| D-068 | 2026-09-22 | Task A Grad-CAM | **初回 commit 前に Grad-CAM documentation のみを修正した**。(1) `gradcam_notes.md` の「総括（定量）」の分母を修正（CL-006）。集計は保存済みの `gradcam_selection.csv` から再計算し、**分子が既存記載と一致することを確認してから分母のみ変更**。3 つの平均は元から 16 例平均でも 15 例平均でもなく「領域指標が算出できた 10 例」の平均だったため、分母を 15 に置き換えるのではなく母集団を明示する記載に改めた。件数は 8 / 15、0 / 15。(2) D-063 の読影総括を `scripts/19` で append（`### 読影総括（研究者記入）`）。**既存の症例別所見・定量情報・実行記録・読影節は削除していない**。**モデル推論・Grad-CAM 再生成・Test 再評価・閾値の再計算・モデル再選択は行っていない**。nan（TN 4 例・FN 1 例）は nan のまま残し、補間・0 置換は禁止事項として明記（Q-E3）。`delta_days_from_T0` が空の件も推測で埋めず未解決のまま記録（Q-E6）。SHA256：修正前 `8448d345…` → 分母修正後 `7c8aa87f…` → 読影総括追記後 `877ad9cc…` | [R] の指示 | nan を 0 に置換する案、分子を推測で変更する案（いずれも禁止） | Grad-CAM 成果物（保存済み。再実行なし） | 確定 |
| D-069 | 2026-09-22 | Task B 解析集団 | **主解析は固定コホート 1,277 例のまま**とし、`visit_concept_name == "Inpatient Visit"` の **955 例**を **pre-specified sensitivity analysis** として並行実施する（研究者承認 2026-09-22）。目的は「inpatient-coded encounters に限定しても主結果が頑健かを確認すること」。flow：臨床ファイル 1,384 例（Inpatient 1,025 / ER 357 / Outpatient 2）→ Task A の index CXR 選定規則を満たした固定コホート 1,277 例（107 例が対象外）→ そのうち Inpatient Visit が **955 例**（Inpatient 1,025 例のうち 70 例が CXR 条件を満たさず固定コホート外）。split 別は Train 759（死亡 134）/ Val 100（16）/ Test 96（17）、除外される ER は 322 例（死亡 2）。固定コホートに Outpatient Visit は 0 例。**主解析を 1,277 例に保つ理由**：Task A（CXR）・Task C（Late Fusion）と同一患者集合での比較と paired DeLong を成立させるため。**感度分析が必要な理由**：Training で ER 262 例の死亡が 1 例（0.38%）、入院例 759 例が 134 例（17.65%）と、院内死亡が構造的にほぼ起こり得ない集団が混在しており、識別能の一部が「入院したか帰宅したか」の再現になりうるため | [R] の承認、Training set の層別集計 | 感度分析を主解析にする案（Task A/C との比較可能性を失うため不採用）、ER 例を除外してコホートを変更する案（固定コホート不変の原則に反するため不採用） | Training（層別集計）。split 別人数は感度分析の実行可能性確認のみに使用し、Test のモデル結果は不使用 | 確定 |
| D-070 | 2026-09-22 | Task B 欠測処理 | **欠測指示変数の採否基準を、変数別の結果を見る前に固定**した（本 Task B の解析内で変数選択を実行する前に事前規定したものであり、研究開始前に登録した pre-registered な基準ではない。英語表記は "pre-specified before variable selection in this Task B run" とする）（`scripts/24_taskB_missing_indicator_decision.py`）。C1 Training 欠測率 >= 5%、C2 入院例内でも欠測と死亡の関連が p<0.05 で維持、C3 他の候補検査がすべて測定済みの入院例（＝フルパネルが出た症例）に限定しても Bonferroni α=0.05/7=0.0071 で維持、C4 欠測が**高い**死亡率と関連（向きの検証）、C5 患者状態を反映する臨床機序が事前に説明できる。**結果：全基準を満たしたのは `lymph`（リンパ球数）のみ**。フルパネル入院例 547 例中 90 例でリンパ球のみ欠測し、死亡率 30.0% 対 16.0%、OR 2.25、p=0.0027。機序：リンパ球数は白血球分画を要し、分画は全血球計算とは別に実施・報告されるため、パネルが出ていても欠けうる。**不採用の理由**：bun・crp・egfr・ddimer は C2/C3 で脱落（フルパネル症例では単独欠測がほぼ存在しない＝パネル未オーダーの副産物）、lactate・troponin はさらに C4 でも脱落（欠測が**低い**死亡率と関連＝「重症を疑わなかったから出さなかった」という診療プロセス情報で、他施設に転移しない）。全 Training で見ると全検査の欠測が OR 0.04〜0.43 と強い保護的関連を示すが、これは ER/入院の層間差がすべてで、層内では符号が反転する（Simpson's paradox） | [R] の指示（一律除外せず変数ごとに評価すること）、Training set のみ | 全検査に指示変数（教員 §4 の字義どおり。層内で有意でなくトリアージの代理のため不採用）、共有「検査パネルなし」指示変数 1 個（143 例中 142 例が ER で入院例内 n=1 のため不採用）、指示変数を一切使わない（当初案。リンパ球の実在する情報を捨てるため不採用） | Training のみ（n=1,021、死亡 135）。**Validation は本判断に使用せず**、ハイパーパラメータ選択・モデル選択・閾値決定に限定。Test は不使用 | 確定（Test 使用前は変更可） |
| D-071 | 2026-09-22 | Task B モデル | **XGBoost の native missing handling を pre-specified secondary model として併走**させる（研究者承認 2026-09-22、教員フィードバック §4 への対応）。**主解析**：3 モデル（LR・XGBoost・MLP）に**同一の入力**（補完済み＋`lymph_missing` 指示変数）を与え、モデル族の違いのみを比較する。**副次モデル**：XGBoost のみ補完も指示変数も行わず欠測を欠測のまま与え、分岐方向を学習させる。**比較目的**：補完という前処理の選択が性能にどれだけ寄与するかを分離すること。**差分は欠測の扱いのみ**で、変数セット・探索空間・seed・評価指標は主解析と同一にする。**選択に使用するデータセット**：ハイパーパラメータ探索は Training 内の repeated stratified CV、モデル選択と閾値決定は Validation。Test は最終評価 1 回のみ。**解釈上の注意**：副次モデルが優れた場合、その差には「検査がオーダーされたか」というトリアージ情報の寄与が含まれうるため、性能差をそのまま前処理の優劣と解釈しない | [R] の承認、教員フィードバック §4 | 全モデルを native missing に揃える案（LR・MLP は欠測を扱えず比較が成立しない）、主解析を native missing にする案（3 モデル比較の公平性が崩れる） | Training（探索）・Validation（選択）。Test は不使用 | 確定 |
| D-072 | 2026-09-22 | Task B データ上の注意 | **性別の欠測が死亡と強く関連する**ことを limitation として記録する。Training 1,021 例中 19 例で `gender_concept_name` が欠測し、そのうち **18 例（94.7%）が死亡**（記録ありでは 11.7%）。19 例は全員が入院例で、18 例が最年少階級 [18,59]。data dictionary には "gender is dropped in select cases for de-identification purposes" と記載があるが、**本欠測がその処理に起因することは確認できていないため、原因を断定しない**。記載は「sex missingness was strongly associated with mortality and may reflect a nonclinical missingness mechanism」とする。**対応**：性別に欠測指示変数を作らない（作れば患者の臨床状態ではない情報でアウトカムをほぼ完全に予測できてしまう）。Training の最頻値（MALE）で補完する。**この注意は Task A・Task C で臨床変数を使う場合にも該当する** | [R] の指示（断定を避ける表現）、Training set | 欠測を独立した水準（Unknown）として保持する案（指示変数と等価になり同じ問題が生じるため不採用）、当該 19 例を除外する案（固定コホート不変の原則に反するため不採用） | Training のみ | 確定 |
| D-073 | 2026-09-22 | Task B 前処理 | **前処理仕様を Training のみで固定**（`scripts/22`、`results/taskB/preprocessing/preprocess_spec.json`）。変換：歪度 >= +1.0 の右裾変数に log1p（rr・bun・crp・lymph・ddimer・lactate）、|歪度| < 0.5 は変換なし＋平均値補完（sbp）、それ以外と左裾は変換なし＋中央値補完（spo2 歪度 −2.10、egfr −0.56）。**log1p を右裾のみに限定した理由**：log は左裾を補正せず悪化させ（SpO2 は −2.10 → −2.46）、補正に必要な反転変換は係数の符号が反転して臨床的な解釈を失わせるため。トロポニンは Training の 78.2%（568/726）が検出限界 0.01 に集中し連続値として扱うと測定系にない精度を仮定することになるため、**検出可否の二値**とする（この判断は値の分布と測定系のみに基づき、アウトカムとの関連は根拠にしていない）。生理学的にあり得ない値 7 件（呼吸数 67/88/95、心拍 6/16、BMI 11.95/92.8）は当該測定値のみ欠測化し、**患者は除外しない**。統計的外れ値の一律除去は行わない。カテゴリは one-hot（drop_first=True、参照水準は age `[18,59]`・sex `FEMALE`・comorbidity `0`）。scaling は LR・MLP のみ（決定木は単調変換に不変） | 教員フィードバック §4・§6、Training set の分布（ヒストグラム・Q-Q plot・歪度・Shapiro-Wilk を併用） | 全連続変数に一律 log1p（左裾を悪化させるため不採用）、Shapiro-Wilk の p 値のみで判定（教員が明示的に禁止） | Training のみ | 確定（Test 使用前は変更可） |
| D-074 | 2026-09-22 | Task B 候補変数 | **候補変数を臨床知識・文献・既存 risk score から事前選定**（`results/taskB/preprocessing/candidate_table.csv`）。骨格に **4C Mortality Score**（Knight et al., BMJ 2020）を採用。理由：COVID-19 院内死亡で最も広く外的妥当性が検証されており、単一の確立したスコアを骨格にすると採否理由が体系的に説明でき、変数数も抑えられるため。4C の 8 項目中 7 項目が利用可能（年齢・性別・併存症数・呼吸数・SpO2・尿素窒素・CRP）で、**意識レベル（GCS）は本データに存在しない**（qSOFA・CURB-65・NEWS2 の意識項目も同様に利用不可）。これに領域を補う形でリンパ球数・D-dimer・乳酸・eGFR・トロポニン・収縮期血圧を追加し、**13 変数・15 パラメータ**とした。**パラメータ予算は Training 死亡 135 / EPV 10 = 13**（教員フィードバックの「約 14」は旧 1,299 コホートの Train 死亡 141 例に基づく値で、現在の固定コホートでは再計算が必要）。候補 15 > 予算 13 のため変数選択を行う。131 列すべてに採否理由を記録し、理由が空欄の列は 0 | 教員フィードバック §1・§2、prior literature、established risk score | 全 115 候補列を投入（EPV を大きく超過）、データ駆動の事前スクリーニング（教員 §2 が明示的に否定） | Training（欠測率のみ）。採否の根拠は臨床知識と文献 | 確定（Test 使用前は変更可） |
| D-075 | 2026-09-22 | Task B Test 運用 | **Test set へのアクセス実績を正確に記録し、以後の運用を分離した**。**これまでに実際に行われたこと**：共有ローダ `load_cohort()` が固定 split 全 1,277 行を読み込んでいたため、scripts/21〜27 の実行時に Test 行がメモリ上に展開され、Test の人数（128）・死亡数（17）・受診形態の構成（入院 96 / ER 32）・在院日数が集計され、`audit_summary.json`・`visit_type_composition.csv`・`inpatient_sensitivity_flow.csv` に記録された。また旧 `scripts/28_taskB_qc.py` は Training で fit した preprocessor を Test に transform していた。**行われていないこと**：Test の特徴量が前処理パラメータ・変数選択・ハイパーパラメータ探索・モデル学習のいずれにも使われておらず、Test 予測も Test 性能指標も生成していない。**したがって「Test predictions have not been generated」は言えるが「Test has never been accessed」は言えない**。**是正**：(1) `load_cohort(splits=...)` と `load_development()` を追加し、Test 承認前のスクリプトは Training + Validation のみを materialise する。(2) QC を pre-Test（`scripts/28_taskB_pre_test_qc.py`、Test 行・ラベル・transform・件数を一切読まない。Test 側は split manifest の ID と split 列のみで検証）と post-Test に分離し、Test を要する 5 項目は deferred として明示。(3) コホート全体の受診形態集計を要する処理は `--cohort-flow` フラグ（既定オフ）に隔離した | [R] の指摘（2026-09-22） | 現状の運用を「Test 未使用」と報告し続ける案（事実と異なるため採らない） | 記録対象は Test のメタデータのみ。Test の特徴量・予測は不使用 | 確定 |
| D-076 | 2026-09-22 | Task B 欠測指示変数 | **指示変数の規則が変数構成と Validation 性能に与える影響を、仕様凍結前に Training/Validation のみで検証**（`scripts/29_taskB_indicator_sensitivity.py`）。3 アーム：A 規則どおり lymph 変数群に強制投入、B 指示変数を独立した選択単位として AIC に委ねる、C 指示変数を作らない。**結果**：A は 12 係数（指示変数あり）、B と C はいずれも 11 係数（AIC が指示変数を採用しない／存在しない）。CV 平均 AUROC は A 0.8890 / B・C 0.8894、Validation AUROC は A 0.8951 / B・C 0.8930、AUPRC は A 0.6346 / B・C 0.6527、Brier はほぼ同一（0.1169 対 0.1172）。**重要な訂正**：当初「指示変数が 1 パラメータを占めたため 4C 構成要素である併存症数が脱落した」と報告したが、これは誤りだった。**3 アームすべてで comorbidity は最終モデルに残らない**（A では予算制約により、B・C では backward AIC 自体が除去）。すなわち comorbidity の脱落は指示変数の規則に起因しない。**判断**：これらの数値を理由に主仕様を変更しない（p 値や性能を見てから規則を変えないという原則）。A（規定どおり）を主仕様として維持し、B・C を感度分析として記録する。指示変数の影響は Validation AUROC で +0.002、AUPRC で −0.018 と小さく、結論を左右しない | [R] の指示（透明化のための感度比較） | 多変量で p=0.96 であることを理由に指示変数を削除する案（[R] が明示的に禁止） | Training（選択・CV）と Validation（性能報告）。Test は不使用 | 確定 |
| D-077 | 2026-09-22 | Task B 表現 | **XGBoost の native missing handling に関する表現を限定**する。「native missing handling に優位性はない」「補完方法が結果を左右していない」という一般化を用いず、**"native missing handling did not improve cross-validated performance compared with the imputation-based approach in this analysis"** と記述する。根拠は CV 平均 AUROC 0.8843（native）対 0.8847（補完）の 1 条件の比較であり、この差から補完方法一般の優劣や他データセットへの一般化は導けない | [R] の指摘 | 「優位性なし」と断定する案（過度の一般化のため採らない） | Training（CV）・Validation | 確定 |
| D-078 | 2026-09-22 | Task B 解釈可能性 | **Feature importance の方法を Test を開く前に固定**する。**主要**：permutation importance（Validation、ROC-AUC の低下量、30 反復、seed 42）。**理由**：線形モデル・木アンサンブル・ニューラルネットを同一の尺度に載せられる唯一の model-agnostic な指標で、3 モデル族を公平に比較できるため。**副次**：LR は標準化係数と OR（95% CI 付き）、XGBoost は native gain importance。**SHAP は採用しない**（モデル族間で値の尺度が揃わず、主要な横断比較に使えないため）。**実行データは Validation**（Test は解釈可能性にも使わない）。**注意書きを必ず併記**：(1) feature importance は causal importance ではない、(2) 相関する変数間では importance が分散するため低い値が臨床的な非重要性を意味しない（本特徴量では bun/egfr、crp/ddimer/lymph が相関）、(3) Validation 128 例・死亡 17 例での算出のため個々の値の不確実性は大きい | methodological consideration、モデル間比較の公平性 | SHAP を主要にする案（尺度が揃わず不採用）、各モデル固有の importance のみ（比較不能のため不採用） | Validation（Test 不使用） | 確定（Test 前に凍結） |
| D-079 | 2026-09-22 | 後続解析 | **患者単位予測ファイルの schema を Test を開く前に固定**（`src/covid_mortality/evaluation/taskB_schema.py`）。Task B の出力先は **`results/taskB/evaluation/test_predictions_taskB.csv`**（未生成）。必須列は `subject_id`（**文字列**、Task A の `results/taskA/evaluation/test_predictions_primary.csv` と同一の列名・同一の値）、`true_label`、`prob_clinical_lr`、`prob_clinical_xgboost`、`prob_clinical_mlp`。任意列は `split`、`prob_clinical_xgboost_native`、閾値適用後の判定、`model_version`、`run_id`。**制約**：患者は 1 行 1 例、subject_id の重複禁止、欠損予測禁止、確率は [0,1]、true_label は固定 split と照合。**患者順序は固定 split manifest の順**（split でフィルタ）とし、その順序の SHA256 を凍結ファイルに記録して後続の merge で検証できるようにする。Task C 完了後に **`results/comparison/test_predictions_all_models.csv`** を作成し、`prob_cxr`・`prob_late_fusion` を加えた 7 列を最終統計比較の source of truth とする。`validate()` が全制約を機械的に検査し、違反があれば例外を投げる | reproducibility consideration、後続の paired DeLong の要件 | 各 Task が独自形式で出力し後から突き合わせる案（型と順序の不一致が起きやすいため不採用） | – | 確定（Test 前に凍結） |
| D-080 | 2026-09-22 | 最終統計比較 | **paired DeLong の比較 family と多重比較補正を、Test 性能を見る前に固定**する。**検定**：paired DeLong（DeLong 1988 / Sun & Xu 2014 の高速実装、同順位は midrank、Task A と同一の `src/covid_mortality/evaluation/delong.py`）。**対応付け**：同一 Test 患者の予測を `subject_id` で merge し、行順を検証したうえで使用する。**独立 2 標本の AUC 比較は使用しない**。**確認的 family（4 比較）**：Late Fusion 対 Clinical LR / Clinical XGBoost / Clinical MLP / CXR ResNet18。**理由**：本研究の確認的な問いは「マルチモーダルな Late Fusion が各単一モダリティを上回るか」であり、family はその 4 比較で閉じるため。単一モダリティ同士の比較は **exploratory** とし、補正なしの p 値である旨を明示して報告する。**補正**：上記 family 内で **Holm 法**（教員フィードバック §10 で現行の Holm 補正が妥当とされている）。**保存項目**：各比較の auc_a、auc_b、AUC 差、差の標準誤差、z、未補正 p、Holm 補正後 p、差の 95% CI。**出力**：`results/comparison/delong_results.json` と `delong_results.csv`。**Test 結果を見てから family や補正方法を変更しない** | 教員フィードバック §10、[R] の指示 | Bonferroni（保守的で検出力が落ちるため不採用）、全 10 通りの総当たりを family にする案（確認的な問いと対応せず検出力を失うため不採用） | – | 確定（Test 前に凍結） |
| D-081 | 2026-09-23 | Task B 解析集団 | **inpatient-only（955 例）の感度分析を実施しないことに決定**（研究者判断 2026-09-23、D-069 の当該部分を撤回）。**主解析は固定 1,277 例に統一**する。**理由**：Task A・Task B・Task C の比較可能性を優先するため。同一患者集合でなければ paired DeLong と Late Fusion が成立しない。**代わりに行うこと**：ER 症例が含まれる事実を cohort description と limitations に明示する（`results/taskB/README.md`）。固定コホート 1,277 例の内訳は Inpatient Visit 955 例（死亡 167、17.5%）／Emergency Room Visit 322 例（死亡 2、0.6%）。Training では ER 262 例中 死亡 1 例、在院日数 中央値 1 日（262 例中 212 例が 1 日）。**将来の追加解析候補として記録**：`visit_concept_name == "Inpatient Visit"` に限定した解析により、識別能のうち病態の重症度に由来する部分と「入院したか帰宅したか」というトリアージ判断の再現に由来する部分を分離できる。**既存資材の扱い**：集団定義のための集計表 2 件（`inpatient_sensitivity_flow.csv`、`visit_type_composition.csv`）は `results/taskB/secondary_exploratory_unused/` へ移動し、**主解析の成果物と混在させない**。当該フォルダの README に「解析は実施しておらず、いかなる結果の算出にも使われていない」ことを明記した。**混同注意**：欠測指示変数の採否基準 C2・C3 は入院例の層内で関連を検証する**診断的手順**であり、主解析の一部として維持される。中止したのは「入院例のみでモデルを再学習・再評価する感度分析」である | [R] の判断（2026-09-23） | 入院例限定を pre-specified secondary として実施する案（D-069。Task A/C との患者集合の相違を避けるため撤回）、ER 例を除外してコホートを変更する案（固定コホート不変の原則に反するため不採用） | Training（層別集計）。モデルの再学習・再評価は行っていない | 確定 |
| D-082 | 2026-09-23 | Task B 変数選択 | **変数選択は Training set のみを用いた backward elimination by AIC で固定**する（研究者の明示的指示 2026-09-23、D-074 を再確認）。**Validation と Test は変数選択に一切使用しない**。選択の単位は元の臨床変数とし、カテゴリ変数のダミーは一括で出し入れする。予算制約（Training 死亡 135 / EPV 10 = 13 係数）を満たすまで AIC コストが最小の変数を除去する。bootstrap 500 回の安定性評価も Training のみで行い、**結果は報告するが選択には使わない**。`scripts/25_taskB_variable_selection.py` は `tb.load_development()` で Training + Validation のみを読み込み、選択処理には `training_only()` の行だけを渡す。`final_variables.json` に `fitted_on: "train only"` を記録し、pre-Test QC に「変数選択が Training のみで行われた」ことの検証項目を追加した。**英語表記**："Variable selection was performed by backward elimination by AIC on the Training set only. Neither the Validation set nor the Test set was used for variable selection." | [R] の指示、教員フィードバック §3、D-074 | Validation を用いた選択（split 規律に反するため不採用）、penalized regression による同時推定（stepwise を求めた教員指示と異なるため不採用） | Training のみ（n=1,021、死亡 135） | 確定 |
| D-083 | 2026-09-23 | 最終統計比較 | **paired DeLong の比較 family の記録を訂正する**（D-080 の family 定義を差し替え）。**正しい設計は 2 family**：**Family A** Late Fusion 対 Clinical LR / Clinical XGBoost / Clinical MLP（3 比較、Family A 内で Holm 補正）、**Family B** Late Fusion 対 Clinical LR / CXR ResNet18（2 比較、Family B 内で別個に Holm 補正）。**D-080 の誤り**：2026-09-22 に「Late Fusion 対 4 モデルの単一 family（4 比較、一括 Holm）」と記録したが、これは合意済みの設計と異なっていた。研究者によれば 2 family 構成が Test 評価前に合意された設計であり、**本件は設計変更ではなく記録の訂正**である。**留意点 1**：`Late Fusion 対 Clinical LR` は両 family に属するため、**Holm 補正後の p 値を 2 つ持つ**（未補正 p 値は同一）。報告時は必ず family を併記し、両者を混同しないこと。**留意点 2（透明性）**：この訂正は Task B の Test 指標が既に存在する時点で行われた。ただし **DeLong 検定は Task B 単独でも他モデルでも一度も実行しておらず**、いずれの family 構成でも補正後 p 値を見ていない。モデル・予測・Test 指標は一切変わらない。**凍結ファイルを編集しない理由**：`results/taskB/modeling/final_selection_taskB.json` の SHA256 は `test_run_meta.json` に記録され、Task B の Test 評価が凍結仕様そのもので実行されたことの証拠になっている。編集するとこの証拠が失われるため、訂正は独立した **`results/comparison/delong_plan.json`** に記載し、凍結ファイルの `delong_plan` キーを上書きする扱いとする | [R] の指摘（2026-09-23） | 凍結ファイルを直接編集する案（Test 実行時ハッシュの証拠性を損なうため不採用）、単一 family のまま実行する案（合意済み設計と異なるため不採用） | – | 確定（DeLong は未実行） |
| D-084 | 2026-09-23 | Task C 前提 | **Task C の設計判断に Test 結果を用いないことを明示的に宣言する**。本エージェントは Task C 開始前に、直前の Task B 比較文書作成を通じて Task A・Task B・Human-guided の Test 結果（Task A 0.8347、Task B LR 0.9465 / XGB 0.9290 / MLP 0.9242、Human-guided LR 0.9412・Late Fusion 0.9444・clinical weight 0.72 など）を既に知っている。この既知性は取り消せないため、**隠さず記録したうえで、すべての設計判断を Training CV と Validation のみから導き、決定ごとに Dataset used を明記する**運用とする。既知の Test 値および Human-guided の fusion weight は**比較用 reference としてのみ**扱い、target にしない。pre-flight audit（`results/taskC/preflight/preflight_audit.{md,json}`）に同文を保存した | [R] の指示（§0・§4）、方法論上の誠実性 | 既知性に触れずに進める案（検証不能な主張になるため不採用） | – | 確定 |
| D-085 | 2026-09-23 | Task C fusion 戦略 | **primary fusion strategy を確率空間の重み付き平均（Late Fusion）に事前固定**する（研究者指示 2026-09-23）。`p_fused = w × p_clinical + (1 − w) × p_cxr`。**logit 空間の重み付き平均と logistic stacking は secondary / exploratory** とし、**primary 戦略の選択のために Validation 上で競わせない**。**Early Fusion は実装しない**。理由：(1) **Task A・Task B とも Training set の予測を保存しておらず**、凍結モデルを再学習せずに out-of-fold 予測を作ることもできないため、combiner を当てはめられるのは Validation の 128 例・死亡 17 例のみ。自由パラメータ 1 個の重み付き平均がこの制約に見合う唯一の形である。(2) Early Fusion は 512 次元の CXR 埋め込み＋12 臨床特徴＝524 入力に対し Training event 135 で、過学習リスクが決定的。(3) 決定段階の統合であり、各モダリティの寄与が重み 1 つで解釈でき ablation も自然に定義できる。**結果として secondary 戦略（logit 空間 Val AUROC 0.9295、stacking 0.9263）は primary（0.9205）より Validation 上で高かったが、事前固定した primary を変更していない**。stacking は 17 event に 3 パラメータを当てはめ同じデータで評価しており楽観的である旨も記録した | [R] の指示、`results/taskC/preflight/preflight_audit.json` の制約記録 | logit 空間を primary にする案、logistic stacking を primary にする案、Early Fusion（いずれも不採用。理由は上記） | Validation のみ（Test 不使用） | 確定 |
| D-086 | 2026-09-23 | Task C 臨床コンポーネント | **臨床コンポーネントを「Training cross-validated ROC-AUC が最良の Task B モデル」という規則で事前に選び、LR を採用**する。CV は 135 event に基づくのに対し Validation は 17 event しかなく、コンポーネント選択の基盤としては CV の方が安定であるため。fused Validation 成績でコンポーネントを選ぶ案は、Validation を当てはめと選択の二重に使うことになるため不採用。Task B の CV AUROC：LR 0.8890 ± 0.037、XGBoost 0.8847 ± 0.039、MLP 0.8796 ± 0.044。**XGBoost・MLP との fusion は secondary として保存**する。**注意**：結果として Human-guided の既知解（Clinical LR + CXR）と同じ構成になったが、選択根拠は上記の事前規定規則のみであり、既知解を target にしていない | [R] の承認、Task B の Training CV | fused Validation 最良で選ぶ案、3 モデルの ensemble を臨床側にする案（いずれも Validation の多重使用のため不採用） | Training cross-validation（選択）、Validation（報告） | 確定 |
| D-087 | 2026-09-23 | Task C 重み選択 | **fusion weight を Validation のみで選択し、w_clinical = 0.75（CXR 重み 0.25）に固定**する。探索空間は w ∈ [0, 1]、刻み 0.05（21 点、端点は単一モダリティに一致）。**主要選択指標は Validation ROC-AUC**、同値時の tie-break は ① PR-AUC ② Brier ③ w が 0.5 に近い方、の順で**探索前に固定**。選択時点で 3 指標すべてが同値の行は 1 件。**重み曲線の平坦さを併記**する：最良 AUROC 0.920509 の 0.005 以内に **6 個の重み**（w = 0.55〜0.80）が入り、17 event で重みを 1 点に定める精度はない。参考値：CXR 単独 0.871754、臨床単独 0.895072、単純平均 0.915209、選択重み 0.920509。**Human-guided の重み 0.72 と近い値になったが、独立した探索の結果であり、既知解を参照していない** | Validation（128 例・死亡 17）のみ | 単純平均 w=0.5 に固定する案（Validation で劣るため不採用だが参照として保存）、より細かい刻み（17 event では分解能がないため不採用） | Validation のみ | 確定 |
| D-088 | 2026-09-23 | Task C 較正の定義 | **ECE の定義を明示的に固定する：10 equal-width bins over [0,1]**。集計は Σ(bin 比率)×|平均予測 − 観測率|。実装は `src/covid_mortality/evaluation/metrics.py` の `expected_calibration_error(y, p, n_bins, scheme)` で、**bin 数と binning scheme を引数として必須**にし、暗黙の既定値を排した。**Validation と Test で同一定義**を用いる。Brier score も併記。Task B で human-guided（10 equal-width）と AI-agent（5 equal-count）の定義不一致が発覚し、XGBoost の ECE が 0.0221 対 0.0749 と大きく変わった経緯（CL-011・D-077 周辺）を踏まえた措置である。bootstrap は Task A・B と同一の **2,000 回・層別・seed 12345**。Human-guided は 10,000 回・seed 42 のため、横断比較用の harmonized CI は**別 artifact** として作成し、既存 official metrics は上書きしない | 教員フィードバック、Task B での実害、reproducibility consideration | 既定値に任せる案（Task B で実害が出たため不採用） | Validation・Test に同一適用 | 確定 |
| D-089 | 2026-09-23 | Task C 凍結 | **Test を開く前に仕様を完全凍結した**：`results/taskC/modeling/final_selection_taskC.json`（SHA256 は QC レポートに記録）。凍結対象は臨床コンポーネント・CXR コンポーネント・fusion 戦略・重み・閾値（Validation Youden 0.325922、tie-break 最小値、**Test で再計算しない**）・較正定義・bootstrap 設定・seed・DeLong 計画・出力スキーマ（`prob_late_fusion` を primary fusion 列とする）・modality importance 手法、および全入力の SHA256。pre-Test QC は **41/41 通過**（`results/taskC/qc/pre_test_qc_report.{csv,json}`）で、Test に依存する 5 項目は deferred として明示。**Test 予測・Test 指標・統合ファイル・DeLong 結果はいずれも未生成**であることを機械的に確認した | 凍結時点の Validation 結果 | – | Validation まで | 確定（Test 実行は人間承認待ち） |
| D-090 | 2026-09-23 | Task C Test 評価 | **凍結仕様のまま Task C の最終 Test 評価を実施した**（研究者承認 2026-09-23）。primary は p_fused = 0.75 x p_clinical_LR + 0.25 x p_cxr、閾値 0.325922（Validation Youden、**Test で再計算せず**）、ECE は 10 equal-width bins、bootstrap 2,000 回・層別・seed 12345。**Test 成績**：ROC-AUC 0.944886（95%% CI 0.892422-0.983042）、PR-AUC 0.780516（0.617560-0.923811）、Brier 0.078034、ECE 0.128711、感度 0.8824、特異度 0.8108、PPV 0.4167、NPV 0.9783、TP/FP/TN/FN 15/21/90/2。**Test 結果を見た後の変更は一切行っていない**：fusion 重み・primary 戦略・臨床コンポーネント・閾値はいずれも凍結値のままで、post-Test QC で fused 列が凍結重みから再現すること（最大差 < 1e-12）と入力 9 件の SHA256 不変を確認した。**secondary/exploratory の Test 結果（logit 空間 0.932697、logistic stacking 0.935877）は primary を上回ったが、primary に昇格させていない**。modality-level permutation importance（Test、post hoc descriptive）は clinical 0.3505 +- 0.0583、cxr 0.0036 +- 0.0062 の AUROC 低下 | [R] の承認、凍結仕様 | Test を見て重みや戦略を変更する案（禁止事項） | Test（最終評価 1 回のみ） | 確定 |
| D-091 | 2026-09-23 | 最終統計比較 | **事前固定した 2 family で paired DeLong を実施**（同一 Test 128 例、死亡 17、subject_id で決定的 merge、独立 2 標本比較は不使用）。**Family A**（Holm、3 比較）：Late Fusion 対 Clinical LR delta -0.001590（p 0.7697、Holm 0.7697）、対 Clinical XGBoost +0.015898（p 0.2271、Holm 0.5398）、対 Clinical MLP +0.020668（p 0.1799、Holm 0.5398）。**Family B**（別 Holm、2 比較）：対 Clinical LR delta -0.001590（p 0.7697、Holm 0.7697）、**対 CXR ResNet18 +0.110228（p 0.002079、Holm 0.004158、有意）**。Late Fusion 対 Clinical LR は両 family に属し未補正 p は同一（0.769705）で、family 列を必須として両方の Holm 補正後 p を保存した。**非有意を equivalence と解釈しない**旨を全比較の interpretation に明記。Test は 128 例・死亡 17 で検出力が限られる | 凍結済み delong_plan.json（D-080・D-083） | – | Test | 確定 |
